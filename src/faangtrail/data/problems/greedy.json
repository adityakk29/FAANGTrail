[
  {
    "id": "gas-station",
    "name": "Gas Station",
    "difficulty": "Medium",
    "description": "There are n gas stations along a circular route, where the amount of gas at the ith station is gas[i]. You have a car with an unlimited gas tank and it costs cost[i] of gas to travel from the ith station to the next (i + 1)th station. You begin the journey with an empty tank at one of the gas stations. Return the starting gas station's index if you can travel around the circuit once in the clockwise direction, otherwise return -1. If a solution exists, it is guaranteed to be unique.",
    "examples": [
      {
        "input": "gas = [1,2,3,4,5], cost = [3,4,5,1,2]",
        "output": "3"
      },
      {
        "input": "gas = [2,3,4], cost = [3,4,3]",
        "output": "-1"
      },
      {
        "input": "gas = [5,1,2,3,4], cost = [4,4,1,5,1]",
        "output": "4"
      }
    ],
    "constraints": "n == gas.length == cost.length. 1 <= n <= 10^5. 0 <= gas[i], cost[i] <= 10^4.",
    "python_template": "# Gas Station\n\nfrom typing import List\n\ndef canCompleteCircuit(gas: List[int], cost: List[int]) -> int:\n    pass\n",
    "go_template": "// Gas Station\n\npackage main\n\nfunc canCompleteCircuit(gas []int, cost []int) int {\n    return -1\n}\n",
    "cpp_template": "// Gas Station\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint canCompleteCircuit(vector<int>& gas, vector<int>& cost) {\n    return 0;\n}\n",
    "java_template": "// Gas Station\n\nclass Solution {\n    public int canCompleteCircuit(int[] gas, int[] cost) {\n        return 0;\n    }\n}\n",
    "js_template": "// Gas Station\n\nfunction canCompleteCircuit(gas, cost) {\n\n}\n",
    "category": "Greedy",
    "review_order": 4,
    "patterns": [
      "core Greedy pattern"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep the invariant explicit.",
    "interview_notes": "Identify the Greedy invariant before coding.",
    "watch_for": "Check boundaries, duplicates, and the smallest valid input."
  },
  {
    "id": "hand-of-straights",
    "name": "Hand of Straights",
    "difficulty": "Medium",
    "description": "Alice has some number of cards given as an integer array hand. She wants to rearrange the cards into groups so that each group is of size groupSize, and consists of groupSize consecutive cards. Return true if she can, otherwise return false.",
    "examples": [
      {
        "input": "hand = [1,2,3,6,2,3,4,7,8], groupSize = 3",
        "output": "true ([1,2,3],[2,3,4],[6,7,8])"
      },
      {
        "input": "hand = [1,2,3,4,5], groupSize = 4",
        "output": "false"
      },
      {
        "input": "hand = [8,10,12], groupSize = 3",
        "output": "false"
      }
    ],
    "constraints": "1 <= hand.length <= 10^4. 0 <= hand[i] <= 10^9. 1 <= groupSize <= hand.length.",
    "python_template": "# Hand of Straights\n\nfrom typing import List\n\ndef isNStraightHand(hand: List[int], groupSize: int) -> bool:\n    pass\n",
    "go_template": "// Hand of Straights\n\npackage main\n\nfunc isNStraightHand(hand []int, groupSize int) bool {\n    return false\n}\n",
    "cpp_template": "// Hand of Straights\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nbool isNStraightHand(vector<int>& hand, int groupSize) {\n    return false;\n}\n",
    "java_template": "// Hand of Straights\n\nclass Solution {\n    public boolean isNStraightHand(int[] hand, int groupSize) {\n        return false;\n    }\n}\n",
    "js_template": "// Hand of Straights\n\nfunction isNStraightHand(hand, groupSize) {\n\n}\n",
    "category": "Greedy",
    "review_order": 5,
    "patterns": [
      "core Greedy pattern"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep the invariant explicit.",
    "interview_notes": "Identify the Greedy invariant before coding.",
    "watch_for": "Check boundaries, duplicates, and the smallest valid input."
  },
  {
    "id": "jump-game-ii",
    "name": "Jump Game II",
    "difficulty": "Medium",
    "description": "You are given a 0-indexed array of integers nums of length n. You are initially positioned at nums[0]. Each element nums[i] represents the maximum length of a forward jump from index i. Return the minimum number of jumps to reach nums[n - 1]. You can assume that you can always reach the last index.",
    "examples": [
      {
        "input": "nums = [2,3,1,1,4]",
        "output": "2 (jump 1 step from 0 to 1, then 3 steps to end)"
      },
      {
        "input": "nums = [2,3,0,1,4]",
        "output": "2"
      },
      {
        "input": "nums = [1,2,3]",
        "output": "2"
      }
    ],
    "constraints": "1 <= nums.length <= 10^4. 0 <= nums[i] <= 1000. It's guaranteed you can reach nums[n - 1].",
    "python_template": "# Jump Game II\n\nfrom typing import List\n\ndef jump(nums: List[int]) -> int:\n    pass\n",
    "go_template": "// Jump Game II\n\npackage main\n\nfunc jump(nums []int) int {\n    return 0\n}\n",
    "cpp_template": "// Jump Game II\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint jump(vector<int>& nums) {\n    return 0;\n}\n",
    "java_template": "// Jump Game II\n\nclass Solution {\n    public int jump(int[] nums) {\n        return 0;\n    }\n}\n",
    "js_template": "// Jump Game II\n\nfunction jump(nums) {\n\n}\n",
    "category": "Greedy",
    "review_order": 7,
    "patterns": [
      "core Greedy pattern"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep the invariant explicit.",
    "interview_notes": "Identify the Greedy invariant before coding.",
    "watch_for": "Check boundaries, duplicates, and the smallest valid input."
  },
  {
    "id": "merge-triplets-to-form-target-triplet",
    "name": "Merge Triplets to Form Target Triplet",
    "difficulty": "Medium",
    "description": "A triplet is an array of three integers. You are given a 2D integer array triplets, where triplets[i] = [ai, bi, ci]. You are also given an integer array target = [x, y, z]. To form the target, you can select some triplets and update the target by taking the element-wise maximum. Return true if it is possible to obtain the target as the element-wise maximum of some subset of triplets.",
    "examples": [
      {
        "input": "triplets = [[2,5,3],[1,8,4],[1,7,5]], target = [2,7,5]",
        "output": "true (pick triplets 0 and 2: max(2,1)=2, max(5,7)=7, max(3,5)=5)"
      },
      {
        "input": "triplets = [[3,4,5],[4,5,6]], target = [3,2,5]",
        "output": "false"
      },
      {
        "input": "triplets = [[2,5,3],[2,3,4],[1,2,5],[5,2,3]], target = [5,5,5]",
        "output": "true"
      }
    ],
    "constraints": "1 <= triplets.length <= 10^5. triplets[i].length == target.length == 3. 1 <= ai, bi, ci, x, y, z <= 1000.",
    "python_template": "# Merge Triplets to Form Target Triplet\n\nfrom typing import List\n\ndef mergeTriplets(triplets: List[List[int]], target: List[int]) -> bool:\n    pass\n",
    "go_template": "// Merge Triplets to Form Target Triplet\n\npackage main\n\nfunc mergeTriplets(triplets [][]int, target []int) bool {\n    return false\n}\n",
    "cpp_template": "// Merge Triplets to Form Target Triplet\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nbool mergeTriplets(vector<vector<int>>& triplets, vector<int>& target) {\n    return false;\n}\n",
    "java_template": "// Merge Triplets to Form Target Triplet\n\nclass Solution {\n    public boolean mergeTriplets(int[][] triplets, int[] target) {\n        return false;\n    }\n}\n",
    "js_template": "// Merge Triplets to Form Target Triplet\n\nfunction mergeTriplets(triplets, target) {\n\n}\n",
    "category": "Greedy",
    "review_order": 8,
    "patterns": [
      "core Greedy pattern"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep the invariant explicit.",
    "interview_notes": "Identify the Greedy invariant before coding.",
    "watch_for": "Check boundaries, duplicates, and the smallest valid input."
  },
  {
    "id": "partition-labels",
    "name": "Partition Labels",
    "difficulty": "Medium",
    "description": "You are given a string s. We want to partition the string into as many parts as possible so that each letter appears in at most one part. Return a list of integers representing the size of these parts. Greedy: track the last occurrence of each character, then expand the current partition until you reach the last occurrence of all characters seen so far.",
    "examples": [
      {
        "input": "s = \"ababcbacadefegdehijhklij\"",
        "output": "[9,7,8]"
      },
      {
        "input": "s = \"eccbbbbdec\"",
        "output": "[10]"
      },
      {
        "input": "s = \"abc\"",
        "output": "[1,1,1]"
      }
    ],
    "constraints": "1 <= s.length <= 500. s consists of lowercase English letters.",
    "python_template": "# Partition Labels\n\nfrom typing import List\n\ndef partitionLabels(s: str) -> List[int]:\n    pass\n",
    "go_template": "// Partition Labels\n\npackage main\n\nfunc partitionLabels(s string) []int {\n    return nil\n}\n",
    "cpp_template": "// Partition Labels\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nvector<int> partitionLabels(string& s) {\n    return {};\n}\n",
    "java_template": "// Partition Labels\n\nclass Solution {\n    public int[] partitionLabels(String s) {\n        return new int[]{};\n    }\n}\n",
    "js_template": "// Partition Labels\n\nfunction partitionLabels(s) {\n\n}\n",
    "category": "Greedy",
    "review_order": 12,
    "patterns": [
      "core Greedy pattern"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep the invariant explicit.",
    "interview_notes": "Identify the Greedy invariant before coding.",
    "watch_for": "Check boundaries, duplicates, and the smallest valid input."
  },
  {
    "id": "valid-parenthesis-string",
    "name": "Valid Parenthesis String",
    "difficulty": "Medium",
    "description": "Given a string s containing only three types of characters: '(', ')' and '*', return true if s is valid. '*' can be treated as a '(', ')' or an empty string. A valid string has every '(' matched by ')' in the correct order. Greedy: track the range of possible open parentheses counts (lo, hi).",
    "examples": [
      {
        "input": "s = \"()\"",
        "output": "true"
      },
      {
        "input": "s = \"(*)\"",
        "output": "true"
      },
      {
        "input": "s = \"(*))\"",
        "output": "true"
      },
      {
        "input": "s = \")(\"",
        "output": "false"
      }
    ],
    "constraints": "1 <= s.length <= 100. s consists of '(', ')' and '*' only.",
    "python_template": "# Valid Parenthesis String\n\ndef checkValidString(s: str) -> bool:\n    pass\n",
    "go_template": "// Valid Parenthesis String\n\npackage main\n\nfunc checkValidString(s string) bool {\n    return false\n}\n",
    "cpp_template": "// Valid Parenthesis String\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nbool checkValidString(string& s) {\n    return false;\n}\n",
    "java_template": "// Valid Parenthesis String\n\nclass Solution {\n    public boolean checkValidString(String s) {\n        return false;\n    }\n}\n",
    "js_template": "// Valid Parenthesis String\n\nfunction checkValidString(s) {\n\n}\n",
    "category": "Greedy",
    "review_order": 14,
    "patterns": [
      "core Greedy pattern"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep the invariant explicit.",
    "interview_notes": "Identify the Greedy invariant before coding.",
    "watch_for": "Check boundaries, duplicates, and the smallest valid input."
  }
]
