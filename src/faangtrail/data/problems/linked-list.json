[
  {
    "id": "reverse-linked-list",
    "name": "Reverse Linked List",
    "difficulty": "Easy",
    "description": "Given the head of a singly linked list, reverse the list and return the reversed list.",
    "examples": [
      {
        "input": "head = [1,2,3,4,5]",
        "output": "[5,4,3,2,1]"
      },
      {
        "input": "head = [1,2]",
        "output": "[2,1]"
      },
      {
        "input": "head = []",
        "output": "[]"
      }
    ],
    "constraints": "The number of nodes is in [0, 5000]. -5000 <= Node.val <= 5000.",
    "python_template": "# Reverse Linked List\n\nfrom typing import Optional\n\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef reverseList(head: Optional[ListNode]) -> Optional[ListNode]:\n    pass\n",
    "go_template": "// Reverse Linked List\n\npackage main\n\ntype ListNode struct {\n\tVal  int\n\tNext *ListNode\n}\n\nfunc reverseList(head *ListNode) *ListNode {\n\n}\n",
    "cpp_template": "// Reverse Linked List\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int val = 0, ListNode* next = nullptr) : val(val), next(next) {}\n};\n\nListNode* reverseList(ListNode* head) {\n    return nullptr;\n}\n",
    "java_template": "// Reverse Linked List\n\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode(int val) { this.val = val; }\n}\n\nclass Solution {\n    public ListNode reverseList(ListNode head) {\n        return null;\n    }\n}\n",
    "js_template": "// Reverse Linked List\n\nclass ListNode {\n    constructor(val) {\n        this.val = val;\n        this.next = null;\n    }\n}\n\nfunction reverseList(head) {\n\n}\n",
    "category": "Linked Lists",
    "review_order": 1,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  },
  {
    "id": "merge-two-sorted-lists",
    "name": "Merge Two Sorted Lists",
    "difficulty": "Easy",
    "description": "Given the heads of two sorted linked lists, merge them into one sorted list by splicing together the nodes. Return the head of the merged list.",
    "examples": [
      {
        "input": "list1 = [1,2,4], list2 = [1,3,4]",
        "output": "[1,1,2,3,4,4]"
      },
      {
        "input": "list1 = [], list2 = []",
        "output": "[]"
      },
      {
        "input": "list1 = [], list2 = [0]",
        "output": "[0]"
      }
    ],
    "constraints": "Both lists are sorted in non-decreasing order. The number of nodes in both lists is in [0, 50]. -100 <= Node.val <= 100.",
    "python_template": "# Merge Two Sorted Lists\n\nfrom typing import Optional\n\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef mergeTwoLists(list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:\n    pass\n",
    "go_template": "// Merge Two Sorted Lists\n\npackage main\n\ntype ListNode struct {\n\tVal  int\n\tNext *ListNode\n}\n\nfunc mergeTwoLists(list1 *ListNode, list2 *ListNode) *ListNode {\n\n}\n",
    "cpp_template": "// Merge Two Sorted Lists\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int val = 0, ListNode* next = nullptr) : val(val), next(next) {}\n};\n\nListNode* mergeTwoLists(ListNode* list1, ListNode* list2) {\n    return nullptr;\n}\n",
    "java_template": "// Merge Two Sorted Lists\n\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode(int val) { this.val = val; }\n}\n\nclass Solution {\n    public ListNode mergeTwoLists(ListNode list1, ListNode list2) {\n        return null;\n    }\n}\n",
    "js_template": "// Merge Two Sorted Lists\n\nclass ListNode {\n    constructor(val) {\n        this.val = val;\n        this.next = null;\n    }\n}\n\nfunction mergeTwoLists(list1, list2) {\n\n}\n",
    "category": "Linked Lists",
    "review_order": 4,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  },
  {
    "id": "linked-list-cycle",
    "name": "Linked List Cycle",
    "difficulty": "Easy",
    "description": "Given head of a linked list, determine if it has a cycle. A cycle exists if some node can be reached again by continuously following the next pointer. Use Floyd's tortoise and hare algorithm.",
    "examples": [
      {
        "input": "head = [3,2,0,-4], pos = 1 (tail connects to node index 1)",
        "output": "true"
      },
      {
        "input": "head = [1,2], pos = 0",
        "output": "true"
      },
      {
        "input": "head = [1], pos = -1 (no cycle)",
        "output": "false"
      }
    ],
    "constraints": "The number of nodes is in [0, 10^4]. -10^5 <= Node.val <= 10^5. pos is -1 or a valid index.",
    "python_template": "# Linked List Cycle\n\nfrom typing import Optional\n\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef hasCycle(head: Optional[ListNode]) -> bool:\n    pass\n",
    "go_template": "// Linked List Cycle\n\npackage main\n\ntype ListNode struct {\n\tVal  int\n\tNext *ListNode\n}\n\nfunc hasCycle(head *ListNode) bool {\n\n}\n",
    "cpp_template": "// Linked List Cycle\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int val = 0, ListNode* next = nullptr) : val(val), next(next) {}\n};\n\nbool hasCycle(ListNode* head) {\n    return false;\n}\n",
    "java_template": "// Linked List Cycle\n\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode(int val) { this.val = val; }\n}\n\nclass Solution {\n    public boolean hasCycle(ListNode head) {\n        return false;\n    }\n}\n",
    "js_template": "// Linked List Cycle\n\nclass ListNode {\n    constructor(val) {\n        this.val = val;\n        this.next = null;\n    }\n}\n\nfunction hasCycle(head) {\n\n}\n",
    "category": "Linked Lists",
    "review_order": 5,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  },
  {
    "id": "remove-nth-node-from-end-of-list",
    "name": "Remove Nth Node From End of List",
    "difficulty": "Medium",
    "description": "Given the head of a linked list, remove the nth node from the end and return the head. Use two-pointer technique with n gap.",
    "examples": [
      {
        "input": "head = [1,2,3,4,5], n = 2",
        "output": "[1,2,3,5]"
      },
      {
        "input": "head = [1], n = 1",
        "output": "[]"
      },
      {
        "input": "head = [1,2], n = 1",
        "output": "[1]"
      }
    ],
    "constraints": "The number of nodes is sz. 1 <= sz <= 30. 0 <= Node.val <= 100. 1 <= n <= sz.",
    "python_template": "# Remove Nth Node From End of List\n\nfrom typing import Optional\n\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef removeNthFromEnd(head: Optional[ListNode], n: int) -> Optional[ListNode]:\n    pass\n",
    "go_template": "// Remove Nth Node From End of List\n\npackage main\n\ntype ListNode struct {\n\tVal  int\n\tNext *ListNode\n}\n\nfunc removeNthFromEnd(head *ListNode, n int) *ListNode {\n\n}\n",
    "cpp_template": "// Remove Nth Node From End of List\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int val = 0, ListNode* next = nullptr) : val(val), next(next) {}\n};\n\nListNode* removeNthFromEnd(ListNode* head, int n) {\n    return nullptr;\n}\n",
    "java_template": "// Remove Nth Node From End of List\n\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode(int val) { this.val = val; }\n}\n\nclass Solution {\n    public ListNode removeNthFromEnd(ListNode head, int n) {\n        return null;\n    }\n}\n",
    "js_template": "// Remove Nth Node From End of List\n\nclass ListNode {\n    constructor(val) {\n        this.val = val;\n        this.next = null;\n    }\n}\n\nfunction removeNthFromEnd(head, n) {\n\n}\n",
    "category": "Linked Lists",
    "review_order": 10,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  },
  {
    "id": "add-two-numbers",
    "name": "Add Two Numbers",
    "difficulty": "Medium",
    "description": "Given two non-empty linked lists representing two non-negative integers stored in reverse order (each node is a single digit), add the two numbers and return the sum as a linked list.",
    "examples": [
      {
        "input": "l1 = [2,4,3], l2 = [5,6,4]",
        "output": "[7,0,8] (342 + 465 = 807)"
      },
      {
        "input": "l1 = [0], l2 = [0]",
        "output": "[0]"
      },
      {
        "input": "l1 = [9,9,9,9,9,9,9], l2 = [9,9,9,9]",
        "output": "[8,9,9,9,0,0,0,1]"
      }
    ],
    "constraints": "The number of nodes in each list is in [1, 100]. 0 <= Node.val <= 9. Numbers do not contain leading zeros except the number 0 itself.",
    "python_template": "# Add Two Numbers\n\nfrom typing import Optional\n\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef addTwoNumbers(l1: Optional[ListNode], l2: Optional[ListNode]) -> Optional[ListNode]:\n    pass\n",
    "go_template": "// Add Two Numbers\n\npackage main\n\ntype ListNode struct {\n\tVal  int\n\tNext *ListNode\n}\n\nfunc addTwoNumbers(l1 *ListNode, l2 *ListNode) *ListNode {\n\n}\n",
    "cpp_template": "// Add Two Numbers\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int val = 0, ListNode* next = nullptr) : val(val), next(next) {}\n};\n\nListNode* addTwoNumbers(ListNode* l1, ListNode* l2) {\n    return nullptr;\n}\n",
    "java_template": "// Add Two Numbers\n\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode(int val) { this.val = val; }\n}\n\nclass Solution {\n    public ListNode addTwoNumbers(ListNode l1, ListNode l2) {\n        return null;\n    }\n}\n",
    "js_template": "// Add Two Numbers\n\nclass ListNode {\n    constructor(val) {\n        this.val = val;\n        this.next = null;\n    }\n}\n\nfunction addTwoNumbers(l1, l2) {\n\n}\n",
    "category": "Linked Lists",
    "review_order": 11,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  },
  {
    "id": "reorder-list",
    "name": "Reorder List",
    "difficulty": "Medium",
    "description": "Given a singly linked list L0 -> L1 -> ... -> Ln-1 -> Ln, reorder it to L0 -> Ln -> L1 -> Ln-1 -> L2 -> Ln-2 -> ... Do it in-place. Hint: find middle, reverse second half, merge alternately.",
    "examples": [
      {
        "input": "head = [1,2,3,4]",
        "output": "[1,4,2,3]"
      },
      {
        "input": "head = [1,2,3,4,5]",
        "output": "[1,5,2,4,3]"
      }
    ],
    "constraints": "The number of nodes is in [1, 5 * 10^4]. 1 <= Node.val <= 1000.",
    "python_template": "# Reorder List\n\nfrom typing import Optional\n\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef reorderList(head: Optional[ListNode]) -> None:\n    \"\"\"Modify in-place.\"\"\"\n    pass\n",
    "go_template": "// Reorder List\n\npackage main\n\ntype ListNode struct {\n\tVal  int\n\tNext *ListNode\n}\n\nfunc reorderList(head *ListNode) {\n\n}\n",
    "cpp_template": "// Reorder List\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int val = 0, ListNode* next = nullptr) : val(val), next(next) {}\n};\n\nvoid reorderList(ListNode* head) {\n\n}\n",
    "java_template": "// Reorder List\n\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode(int val) { this.val = val; }\n}\n\nclass Solution {\n    public void reorderList(ListNode head) {\n\n    }\n}\n",
    "js_template": "// Reorder List\n\nclass ListNode {\n    constructor(val) {\n        this.val = val;\n        this.next = null;\n    }\n}\n\nfunction reorderList(head) {\n\n}\n",
    "category": "Linked Lists",
    "review_order": 14,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  },
  {
    "id": "copy-list-with-random-pointer",
    "name": "Copy List with Random Pointer",
    "difficulty": "Medium",
    "description": "Given a linked list where each node has a next pointer and a random pointer (which can point to any node or null), construct a deep copy of the list. Return the head of the copied list.",
    "examples": [
      {
        "input": "head = [[7,null],[13,0],[11,4],[10,2],[1,0]]",
        "output": "Deep copy of the list"
      },
      {
        "input": "head = [[1,1],[2,1]]",
        "output": "Deep copy of the list"
      }
    ],
    "constraints": "0 <= n <= 1000. -10^4 <= Node.val <= 10^4. Node.random is null or points to a node in the list.",
    "python_template": "# Copy List with Random Pointer\n\nfrom typing import Optional\n\nclass Node:\n    def __init__(self, val=0, next=None, random=None):\n        self.val = val\n        self.next = next\n        self.random = random\n\ndef copyRandomList(head: Optional[Node]) -> Optional[Node]:\n    pass\n",
    "go_template": "// Copy List with Random Pointer\n\npackage main\n\ntype Node struct {\n\tVal    int\n\tNext   *Node\n\tRandom *Node\n}\n\nfunc copyRandomList(head *Node) *Node {\n\n}\n",
    "cpp_template": "// Copy List with Random Pointer\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct Node {\n    int val;\n    Node* next;\n    Node* random;\n    Node(int val = 0, Node* next = nullptr, Node* random = nullptr) : val(val), next(next), random(random) {}\n};\n\nNode* copyRandomList(Node* head) {\n    return nullptr;\n}\n",
    "java_template": "// Copy List with Random Pointer\n\nclass Node {\n    int val;\n    Node next;\n    Node random;\n    Node(int val) { this.val = val; }\n}\n\nclass Solution {\n    public Node copyRandomList(Node head) {\n        return null;\n    }\n}\n",
    "js_template": "// Copy List with Random Pointer\n\nclass Node {\n    constructor(val) {\n        this.val = val;\n        this.next = null;\n        this.random = null;\n    }\n}\n\nfunction copyRandomList(head) {\n\n}\n",
    "category": "Linked Lists",
    "review_order": 16,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  },
  {
    "id": "find-the-duplicate-number",
    "name": "Find the Duplicate Number",
    "difficulty": "Medium",
    "description": "Given an array of n + 1 integers where each integer is in [1, n], there is exactly one repeated number. Find it without modifying the array and using O(1) extra space. Hint: treat indices as linked list pointers and use Floyd's cycle detection.",
    "examples": [
      {
        "input": "nums = [1,3,4,2,2]",
        "output": "2"
      },
      {
        "input": "nums = [3,1,3,4,2]",
        "output": "3"
      },
      {
        "input": "nums = [3,3,3,3,3]",
        "output": "3"
      }
    ],
    "constraints": "1 <= n <= 10^5. nums.length == n + 1. 1 <= nums[i] <= n. There is only one repeated number.",
    "python_template": "# Find the Duplicate Number\n\nfrom typing import List\n\ndef findDuplicate(nums: List[int]) -> int:\n    pass\n",
    "go_template": "// Find the Duplicate Number\n\npackage main\n\nfunc findDuplicate(nums []int) int {\n\n}\n",
    "cpp_template": "// Find the Duplicate Number\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint findDuplicate(vector<int>& nums) {\n    return 0;\n}\n",
    "java_template": "// Find the Duplicate Number\n\nclass Solution {\n    public int findDuplicate(int[] nums) {\n        return 0;\n    }\n}\n",
    "js_template": "// Find the Duplicate Number\n\nfunction findDuplicate(nums) {\n\n}\n",
    "category": "Linked Lists",
    "review_order": 17,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  },
  {
    "id": "lru-cache",
    "name": "LRU Cache",
    "difficulty": "Medium",
    "description": "Design a data structure that follows the constraints of a Least Recently Used (LRU) cache. Implement get(key) and put(key, value). Both must run in O(1) time. When the cache reaches capacity, evict the least recently used key before inserting a new one.",
    "examples": [
      {
        "input": "LRUCache(2), put(1,1), put(2,2), get(1)=1, put(3,3), get(2)=-1, put(4,4), get(1)=-1, get(3)=3, get(4)=4",
        "output": "See above"
      }
    ],
    "constraints": "1 <= capacity <= 3000. 0 <= key <= 10^4. 0 <= value <= 10^5. At most 2 * 10^5 calls to get and put.",
    "python_template": "# LRU Cache\n\nclass LRUCache:\n    def __init__(self, capacity: int):\n        pass\n\n    def get(self, key: int) -> int:\n        pass\n\n    def put(self, key: int, value: int) -> None:\n        pass\n",
    "go_template": "// LRU Cache\n\npackage main\n\ntype LRUCache struct {\n}\n\nfunc Constructor(capacity int) LRUCache {\n\treturn LRUCache{}\n}\n\nfunc (c *LRUCache) Get(key int) int {\n\treturn -1\n}\n\nfunc (c *LRUCache) Put(key int, value int) {\n}\n",
    "cpp_template": "// LRU Cache\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nclass LRUCache {\npublic:\n    LRUCache(int capacity) {\n\n    }\n\n    int Get(int key) {\n        return 0;\n    }\n\n    void Put(int key, int value) {\n\n    }\n};\n",
    "java_template": "// LRU Cache\n\nimport java.util.*;\n\nclass LRUCache {\n    public LRUCache(int capacity) {\n\n    }\n\n    public int Get(int key) {\n        return 0;\n    }\n\n    public void Put(int key, int value) {\n\n    }\n}\n",
    "js_template": "// LRU Cache\n\nclass LRUCache {\n    constructor(capacity) {\n\n    }\n\n    get(key) {\n\n    }\n\n    put(key, value) {\n\n    }\n}\n",
    "category": "Linked Lists",
    "review_order": 20,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  },
  {
    "id": "merge-k-sorted-lists",
    "name": "Merge K Sorted Lists",
    "difficulty": "Hard",
    "description": "Given an array of k linked lists, each sorted in ascending order, merge all into one sorted linked list and return it. Use a min-heap or divide-and-conquer.",
    "examples": [
      {
        "input": "lists = [[1,4,5],[1,3,4],[2,6]]",
        "output": "[1,1,2,3,4,4,5,6]"
      },
      {
        "input": "lists = []",
        "output": "[]"
      },
      {
        "input": "lists = [[]]",
        "output": "[]"
      }
    ],
    "constraints": "k == lists.length. 0 <= k <= 10^4. 0 <= lists[i].length <= 500. -10^4 <= lists[i][j] <= 10^4. Each list is sorted in ascending order. Sum of all lists[i].length <= 10^4.",
    "python_template": "# Merge K Sorted Lists\n\nfrom typing import List, Optional\nimport heapq\n\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef mergeKLists(lists: List[Optional[ListNode]]) -> Optional[ListNode]:\n    pass\n",
    "go_template": "// Merge K Sorted Lists\n\npackage main\n\nimport \"container/heap\"\n\ntype ListNode struct {\n\tVal  int\n\tNext *ListNode\n}\n\nfunc mergeKLists(lists []*ListNode) *ListNode {\n\n}\n",
    "cpp_template": "// Merge K Sorted Lists\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int val = 0, ListNode* next = nullptr) : val(val), next(next) {}\n};\n\nListNode* mergeKLists(vector<ListNode*>& lists) {\n    return nullptr;\n}\n",
    "java_template": "// Merge K Sorted Lists\n\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode(int val) { this.val = val; }\n}\n\nclass Solution {\n    public ListNode mergeKLists(ListNode[] lists) {\n        return null;\n    }\n}\n",
    "js_template": "// Merge K Sorted Lists\n\nclass ListNode {\n    constructor(val) {\n        this.val = val;\n        this.next = null;\n    }\n}\n\nfunction mergeKLists(lists) {\n\n}\n",
    "category": "Linked Lists",
    "review_order": 27,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  },
  {
    "id": "reverse-nodes-in-k-group",
    "name": "Reverse Nodes in k-Group",
    "difficulty": "Hard",
    "description": "Given the head of a linked list, reverse the nodes k at a time and return the modified list. If the number of remaining nodes is less than k, leave them as-is. You may not alter the values, only the nodes.",
    "examples": [
      {
        "input": "head = [1,2,3,4,5], k = 2",
        "output": "[2,1,4,3,5]"
      },
      {
        "input": "head = [1,2,3,4,5], k = 3",
        "output": "[3,2,1,4,5]"
      }
    ],
    "constraints": "The number of nodes is n. 1 <= k <= n <= 5000. 0 <= Node.val <= 1000.",
    "python_template": "# Reverse Nodes in k-Group\n\nfrom typing import Optional\n\nclass ListNode:\n    def __init__(self, val=0, next=None):\n        self.val = val\n        self.next = next\n\ndef reverseKGroup(head: Optional[ListNode], k: int) -> Optional[ListNode]:\n    pass\n",
    "go_template": "// Reverse Nodes in k-Group\n\npackage main\n\ntype ListNode struct {\n\tVal  int\n\tNext *ListNode\n}\n\nfunc reverseKGroup(head *ListNode, k int) *ListNode {\n\n}\n",
    "cpp_template": "// Reverse Nodes in k-Group\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nstruct ListNode {\n    int val;\n    ListNode* next;\n    ListNode(int val = 0, ListNode* next = nullptr) : val(val), next(next) {}\n};\n\nListNode* reverseKGroup(ListNode* head, int k) {\n    return nullptr;\n}\n",
    "java_template": "// Reverse Nodes in k-Group\n\nclass ListNode {\n    int val;\n    ListNode next;\n    ListNode(int val) { this.val = val; }\n}\n\nclass Solution {\n    public ListNode reverseKGroup(ListNode head, int k) {\n        return null;\n    }\n}\n",
    "js_template": "// Reverse Nodes in k-Group\n\nclass ListNode {\n    constructor(val) {\n        this.val = val;\n        this.next = null;\n    }\n}\n\nfunction reverseKGroup(head, k) {\n\n}\n",
    "category": "Linked Lists",
    "review_order": 28,
    "patterns": [
      "pointer manipulation"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Use sentinel nodes or slow/fast pointers to make structural changes predictable.",
    "watch_for": "Save next pointers before rewiring and preserve head or cycle edge cases."
  }
]
