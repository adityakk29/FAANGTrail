[
  {
    "id": "best-time-to-buy-and-sell-stock",
    "name": "Best Time to Buy and Sell Stock",
    "difficulty": "Easy",
    "description": "You are given an array prices where prices[i] is the price of a stock on the ith day. You want to maximize profit by choosing a single day to buy and a single day in the future to sell. Return the maximum profit, or 0 if no profit is possible.",
    "examples": [
      {
        "input": "prices = [7,1,5,3,6,4]",
        "output": "5 (buy at 1, sell at 6)"
      },
      {
        "input": "prices = [7,6,4,3,1]",
        "output": "0 (no profitable transaction)"
      }
    ],
    "constraints": "1 <= prices.length <= 10^5. 0 <= prices[i] <= 10^4.",
    "python_template": "# Best Time to Buy and Sell Stock\n\nfrom typing import List\n\ndef maxProfit(prices: List[int]) -> int:\n    pass\n",
    "go_template": "// Best Time to Buy and Sell Stock\n\npackage main\n\nfunc maxProfit(prices []int) int {\n\n}\n",
    "cpp_template": "// Best Time to Buy and Sell Stock\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint maxProfit(vector<int>& prices) {\n    return 0;\n}\n",
    "java_template": "// Best Time to Buy and Sell Stock\n\nclass Solution {\n    public int maxProfit(int[] prices) {\n        return 0;\n    }\n}\n",
    "js_template": "// Best Time to Buy and Sell Stock\n\nfunction maxProfit(prices) {\n\n}\n",
    "category": "Sliding Window",
    "review_order": 1,
    "patterns": [
      "sliding window"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Expand the right edge, restore the window invariant from the left, and record valid windows.",
    "watch_for": "Update counts before shrinking and distinguish exactly-k from at-most-k windows."
  },
  {
    "id": "longest-substring-without-repeating-characters",
    "name": "Longest Substring Without Repeating Characters",
    "difficulty": "Medium",
    "description": "Given a string s, find the length of the longest substring without repeating characters.",
    "examples": [
      {
        "input": "s = \"abcabcbb\"",
        "output": "3 (\"abc\")"
      },
      {
        "input": "s = \"bbbbb\"",
        "output": "1 (\"b\")"
      },
      {
        "input": "s = \"pwwkew\"",
        "output": "3 (\"wke\")"
      }
    ],
    "constraints": "0 <= s.length <= 5 * 10^4. s consists of English letters, digits, symbols and spaces.",
    "python_template": "# Longest Substring Without Repeating Characters\n\ndef lengthOfLongestSubstring(s: str) -> int:\n    pass\n",
    "go_template": "// Longest Substring Without Repeating Characters\n\npackage main\n\nfunc lengthOfLongestSubstring(s string) int {\n\n}\n",
    "cpp_template": "// Longest Substring Without Repeating Characters\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint lengthOfLongestSubstring(string& s) {\n    return 0;\n}\n",
    "java_template": "// Longest Substring Without Repeating Characters\n\nclass Solution {\n    public int lengthOfLongestSubstring(String s) {\n        return 0;\n    }\n}\n",
    "js_template": "// Longest Substring Without Repeating Characters\n\nfunction lengthOfLongestSubstring(s) {\n\n}\n",
    "category": "Sliding Window",
    "review_order": 2,
    "patterns": [
      "sliding window"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Expand the right edge, restore the window invariant from the left, and record valid windows.",
    "watch_for": "Update counts before shrinking and distinguish exactly-k from at-most-k windows."
  },
  {
    "id": "longest-repeating-character-replacement",
    "name": "Longest Repeating Character Replacement",
    "difficulty": "Medium",
    "description": "Given a string s and an integer k, you can choose any character and change it to any other uppercase English character at most k times. Return the length of the longest substring containing the same letter after performing at most k changes.",
    "examples": [
      {
        "input": "s = \"ABAB\", k = 2",
        "output": "4 (replace both A's or both B's)"
      },
      {
        "input": "s = \"AABABBA\", k = 1",
        "output": "4 (replace one B to get \"AABA\" -> \"AAAA\")"
      }
    ],
    "constraints": "1 <= s.length <= 10^5. s consists of uppercase English letters. 0 <= k <= s.length.",
    "python_template": "# Longest Repeating Character Replacement\n\ndef characterReplacement(s: str, k: int) -> int:\n    pass\n",
    "go_template": "// Longest Repeating Character Replacement\n\npackage main\n\nfunc characterReplacement(s string, k int) int {\n\n}\n",
    "cpp_template": "// Longest Repeating Character Replacement\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nint characterReplacement(string& s, int k) {\n    return 0;\n}\n",
    "java_template": "// Longest Repeating Character Replacement\n\nclass Solution {\n    public int characterReplacement(String s, int k) {\n        return 0;\n    }\n}\n",
    "js_template": "// Longest Repeating Character Replacement\n\nfunction characterReplacement(s, k) {\n\n}\n",
    "category": "Sliding Window",
    "review_order": 3,
    "patterns": [
      "sliding window"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Expand the right edge, restore the window invariant from the left, and record valid windows.",
    "watch_for": "Update counts before shrinking and distinguish exactly-k from at-most-k windows."
  },
  {
    "id": "minimum-window-substring",
    "name": "Minimum Window Substring",
    "difficulty": "Hard",
    "description": "Given two strings s and t, return the minimum window substring of s such that every character in t (including duplicates) is included in the window. If no such substring exists, return \"\".",
    "examples": [
      {
        "input": "s = \"ADOBECODEBANC\", t = \"ABC\"",
        "output": "\"BANC\""
      },
      {
        "input": "s = \"a\", t = \"a\"",
        "output": "\"a\""
      },
      {
        "input": "s = \"a\", t = \"aa\"",
        "output": "\"\" (t has 2 a's but s has only 1)"
      }
    ],
    "constraints": "1 <= s.length, t.length <= 10^5. s and t consist of uppercase and lowercase English letters.",
    "python_template": "# Minimum Window Substring\n\ndef minWindow(s: str, t: str) -> str:\n    pass\n",
    "go_template": "// Minimum Window Substring\n\npackage main\n\nfunc minWindow(s string, t string) string {\n\n}\n",
    "cpp_template": "// Minimum Window Substring\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nstring minWindow(string& s, string& t) {\n    return \"\";\n}\n",
    "java_template": "// Minimum Window Substring\n\nclass Solution {\n    public String minWindow(String s, String t) {\n        return \"\";\n    }\n}\n",
    "js_template": "// Minimum Window Substring\n\nfunction minWindow(s, t) {\n\n}\n",
    "category": "Sliding Window",
    "review_order": 4,
    "patterns": [
      "sliding window"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Expand the right edge, restore the window invariant from the left, and record valid windows.",
    "watch_for": "Update counts before shrinking and distinguish exactly-k from at-most-k windows."
  },
  {
    "id": "permutation-in-string",
    "name": "Permutation in String",
    "difficulty": "Medium",
    "description": "Given two strings s1 and s2, return true if s2 contains a permutation of s1, or false otherwise. In other words, return true if one of s1's permutations is a substring of s2.",
    "examples": [
      {
        "input": "s1 = \"ab\", s2 = \"eidbaooo\"",
        "output": "true (s2 contains \"ba\")"
      },
      {
        "input": "s1 = \"ab\", s2 = \"eidboaoo\"",
        "output": "false"
      }
    ],
    "constraints": "1 <= s1.length, s2.length <= 10^4. s1 and s2 consist of lowercase English letters.",
    "python_template": "# Permutation in String\n\ndef checkInclusion(s1: str, s2: str) -> bool:\n    pass\n",
    "go_template": "// Permutation in String\n\npackage main\n\nfunc checkInclusion(s1 string, s2 string) bool {\n\n}\n",
    "cpp_template": "// Permutation in String\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nbool checkInclusion(string& s1, string& s2) {\n    return false;\n}\n",
    "java_template": "// Permutation in String\n\nclass Solution {\n    public boolean checkInclusion(String s1, String s2) {\n        return false;\n    }\n}\n",
    "js_template": "// Permutation in String\n\nfunction checkInclusion(s1, s2) {\n\n}\n",
    "category": "Sliding Window",
    "review_order": 13,
    "patterns": [
      "sliding window"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Expand the right edge, restore the window invariant from the left, and record valid windows.",
    "watch_for": "Update counts before shrinking and distinguish exactly-k from at-most-k windows."
  },
  {
    "id": "sliding-window-maximum",
    "name": "Sliding Window Maximum",
    "difficulty": "Hard",
    "description": "Given an array of integers nums and a sliding window of size k moving from left to right, return the max value in each window position. Use a deque for O(n) solution.",
    "examples": [
      {
        "input": "nums = [1,3,-1,-3,5,3,6,7], k = 3",
        "output": "[3,3,5,5,6,7]"
      },
      {
        "input": "nums = [1], k = 1",
        "output": "[1]"
      }
    ],
    "constraints": "1 <= nums.length <= 10^5. -10^4 <= nums[i] <= 10^4. 1 <= k <= nums.length.",
    "python_template": "# Sliding Window Maximum\n\nfrom typing import List\nfrom collections import deque\n\ndef maxSlidingWindow(nums: List[int], k: int) -> List[int]:\n    pass\n",
    "go_template": "// Sliding Window Maximum\n\npackage main\n\nfunc maxSlidingWindow(nums []int, k int) []int {\n\n}\n",
    "cpp_template": "// Sliding Window Maximum\n\n#include <bits/stdc++.h>\nusing namespace std;\n\nvector<int> maxSlidingWindow(vector<int>& nums, int k) {\n    return {};\n}\n",
    "java_template": "// Sliding Window Maximum\n\nclass Solution {\n    public int[] maxSlidingWindow(int[] nums, int k) {\n        return new int[]{};\n    }\n}\n",
    "js_template": "// Sliding Window Maximum\n\nfunction maxSlidingWindow(nums, k) {\n\n}\n",
    "category": "Sliding Window",
    "review_order": 14,
    "patterns": [
      "sliding window"
    ],
    "complexity_target": "Prefer the optimal stated complexity and keep auxiliary state proportional to the chosen technique.",
    "interview_notes": "Expand the right edge, restore the window invariant from the left, and record valid windows.",
    "watch_for": "Update counts before shrinking and distinguish exactly-k from at-most-k windows."
  }
]
